# 操作总结

## 背景
仓库在推送到 GitHub 时失败，主要原因是历史提交中包含超过 GitHub 限制的大文件，尤其是 PDF 文件。

## 已执行的关键步骤
1. 检查仓库体积与最大文件，确认存在多个大文件。
2. 查看 `.gitignore`，确认原先只忽略了个别文件，未覆盖历史中的所有 PDF。
3. 定位导致推送失败的超大 PDF 文件。
4. 使用历史重写方式，把提交历史中的所有 PDF 从 Git 记录中移除。
5. 重新推送到远端，推送成功。
6. 之后发现本地 PDF 文件显示不见，又从重写前的原始历史引用中把本地 PDF 恢复回工作区。

## 执行的指令
以下是本次处理过程中实际执行的主要指令：

1. git count-objects -vH
2. git ls-files
3. git remote -v
4. git log --oneline --stat -- 文件名
5. git filter-branch --force --index-filter "git rm -r --cached --ignore-unmatch -- ':(glob)**/*.pdf'" --prune-empty --tag-name-filter cat -- --all
6. git push -u origin main
7. git restore --source=refs/original/refs/heads/main --worktree -- ':(glob)**/*.pdf'

说明：
- 第 5 条用于清理历史中的 PDF，但不会删除本地工作区文件。
- 第 7 条用于把本地 PDF 从原始历史中恢复回来。
- 如果后续再次推送，PDF 不会再进入新的 Git 历史。

## 当前结果
- 远端仓库已成功更新。
- 本地 PDF 文件已恢复。
- 当前工作区保持干净，没有未提交改动。

## 备注
- `.gitignore` 只能影响未被跟踪的新文件，不能自动解决已经进入历史的超大文件。
- 如果以后还要继续添加大文件，建议优先使用 Git LFS，或者避免把生成物直接提交到仓库。